# Git Worktree Convention

- Create worktrees under `.claude/worktrees/` (not `.worktrees/` or `worktrees/`)
- This directory is not tracked by git
- Do not add worktree-related entries to `.gitignore`
