---
description: Conventions for .claude/ configuration files (rules, skills)
paths: ['.claude/rules/**', '.claude/skills/**']
---

# Claude Configuration Conventions

## Language

- All files under `.claude/rules/` and `.claude/skills/` must be written in English
- This includes frontmatter (`name`, `description`), headings, and body content
